name: tonic_synth_flutter
description: "Flutter App with Tonic audio synthesis library"
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  ffi: ^2.2.0
  code_assets: ^1.2.1
  hooks: ^2.0.2
  flutter_soloud: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1

flutter:  
  uses-material-design: true